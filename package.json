{
  "name": "pi-plugins",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pi extensions monorepo (see each plugin directory).",
  "dependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  }
}
